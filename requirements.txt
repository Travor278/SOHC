# CRAIC2026 方向三 — Python 依赖（PyTorch 主栈 + TF inference 子进程）
# Python: 推荐 3.10 或 3.11
# 安装：pip install -r requirements.txt
# 注意：mamba-ssm 需要 CUDA 11.8+，CPU-only 环境请先注释掉 mamba-ssm 这一行

# === 核心 DL ===
torch>=2.1,<2.5
numpy>=1.24,<2.0
scipy>=1.11
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13

# === Mamba 世界模型 (W2) ===
mamba-ssm>=2.0.3
causal-conv1d>=1.4.0   # mamba-ssm 依赖

# === RL (W3) ===
stable-baselines3>=2.3
gymnasium>=0.29

# === SOC 估计器：KeiLongW 是 TF/Keras，单独 inference 子进程 ===
tensorflow>=2.13,<2.16

# === SOH 估计器：BatteryML 依赖 ===
xgboost>=2.0
PyYAML>=6.0
tqdm>=4.66
h5py>=3.10

# === 工具 ===
hydra-core>=1.3
optuna>=3.5
einops>=0.7
tensorboard>=2.13
jupyter>=1.0
pytest>=8.0

# === MATLAB / Simulink 数据互通（可选，仅 W5）===
# scipy.io 已含 .mat 读写，无需额外
